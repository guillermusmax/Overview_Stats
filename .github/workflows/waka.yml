name: Waka Readme
 
on:
  push:
    branches:
      - main
  workflow_dispatch:
  schedule:
    # Runs at 12am UTC
    - cron: '5 * * * *'
 
jobs:
  update-readme:
    name: Update this repo's README 
    runs-on: ubuntu-latest
    steps:
      - uses: A2C29K9/A2C29K9@main
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
